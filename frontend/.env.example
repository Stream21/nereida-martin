# Opcional en desarrollo: Vite hace proxy de /api a localhost:3000 automáticamente.
# En producción, déjalo vacío si el frontend se sirve desde el mismo dominio que el backend.
# VITE_API_URL=http://localhost:3000
